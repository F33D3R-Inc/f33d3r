<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production is HTTPS only. Nothing this app sends — a session token, a sealed
         envelope, a signed work — may cross a cleartext connection. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- The emulator's loopback to a local nantar. Debug builds only: this block is
         removed from the release manifest by the build. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
