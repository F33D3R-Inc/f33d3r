[server]
host = "0.0.0.0"
port = 8079

[database]
url              = "postgres://f33d3r:change_me@localhost:5432/f33d3r_registry"
max_connections  = 10
connect_timeout  = 5

[registry]
# How long before a brain is considered stale (seconds)
stale_threshold_secs = 90
# Whether to enforce strict backward-compatibility checks
strict_compatibility = true
