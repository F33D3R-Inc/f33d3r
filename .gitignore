# Build output and local toolchain binaries
dist/
.bin/
target/
__pycache__/
*.pyc
build/
.gradle/

# Written by the runtime beside the app
facet-uploads/
*.db
*.sqlite

# Vendored remote facets (facet.lock IS committed — this cache is not)
facet_modules/

# Local configuration and secrets — never commit these
.env
.env.local
infra/tor/hs_ed25519_secret_key
